支持使用两种网络结构alexnet和googLeNet来训练车牌二分类；
在start_train.bat文件中可以选择使用哪种进行训练；
经过测试验证：
（1）在将图像格式转换为leveldb时，在convert_imagenet.bat文件中可以选择对图像归一化的大小
目前默认大小为256，由于在alextnet和googlenet的网络参数中都需要设置参数：crop_size大小，都是224，所以归一化的时候一定要大于这个数值。

（2）将图像归一化到256大小时，网络结构的batch_size大小必须设置小一些，否则会造成内存不足，目前是用的值是1；
